[build]
command = "python3 -m src.cli static --output-dir site --max-workers 8 --include-all-resorts"
publish = "site"

[build.environment]
PYTHON_VERSION = "3.11"

# Keep Git-driven deploys preview-only. Netlify treats an ignore command that
# exits successfully as a signal to skip the build for that deploy context.
[context.production]
ignore = "exit 0"

[context.branch-deploy]
ignore = "exit 0"

[context.deploy-preview]
# Mock weather exercises both palettes without changing the production forecast.
command = "python3 -m src.cli static --output-dir site --max-workers 8 --include-all-resorts && python3 scripts/build_theme_preview.py --output-dir site/theme-preview"
publish = "site"
